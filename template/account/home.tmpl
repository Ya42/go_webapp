{{define "title"}}Welcom{{end}}
{{define "head"}}{{end}}
{{define "content"}}

<div class="container">
	<div class="page-header">
		<h1>{{template "title" .}}</h1>
 <h2>Welcome {{.firstname}}</h2>
	<p style="margin-top: 15px;">
	{{LINK "meeting/index" "Go to my meetings"}}
	</p>

	{{template "footer" .}}
</div>

{{end}}
{{define "foot"}}{{end}}
